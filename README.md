# Lint scripts for AMWA NMOS specs

(c) AMWA 2020

This repo holds scripts used to lint documentation, RAML and JSON for NMOS specification repos.

These are invoked (using CI) on dev branches of repos.
